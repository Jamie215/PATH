---
import Base from '../../layouts/Base.astro';
import FreBAQSurvey from '../../components/FreBAQSurvey.svelte';
import BackLink from '../../components/BackLink.svelte';
---
<Base title="Body Awareness · Survey" description="Body Awareness — survey" wide>
  <BackLink client:only="svelte" />
  <h1 class="survey-heading">Symptom assessment</h1>
  <FreBAQSurvey client:only="svelte" />
</Base>

<style>
  .survey-heading {
    margin-bottom: var(--space-3);
  }
</style>
